<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="spatula.dao.reference.OrganizationDao">

    <!-- <insert id="create" parameterType="organization" keyProperty="id">
        INSERT INTO
            ${db.schema}.organizations
                (name, org_form_id, bin, address, iik, bank_id, bik)
        VALUES
            (#{name}, #{orgFormId}, #{bin}, #{address}, #{iik}, #{bankId}, #{bik})
    </insert>

    <select id="read" parameterType="long" resultType="Organization">
        SELECT
            id, name, org_form_id, bin, address, iik, bank_id, bik
        FROM
            ${db.schema}.organizations
        WHERE
            id = #{id}
    </select>

    <update id="update" parameterType="Organization">
        UPDATE
            ${db.schema}.organizations
        SET
            name = #{name},
            org_form_id = #{orgFormId},
            bin = #{bin},
            address = #{address},
            iik = #{iik},
            bank_id = #{bankId},
            bin = #{bin}
        WHERE
            id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM
            ${db.schema}.organizations
        WHERE
            id = #{id}
    </delete>

    <select id="findAll" resultType="Organization">
        SELECT
            id, name, org_form_id, bin, address, iik, bank_id, bik
        FROM
            ${db.schema}.organizations
    </select>
 -->
</mapper>
