<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="spatula.dao.reference.OrganizationDao">

    <insert id="create" parameterType="organization" keyProperty="id">
        INSERT INTO
            ${db.schema}.organizations
                (name, org_form_id, bin, address, bank_account_id, email)
        VALUES
            (#{name}, #{orgFormId}, #{bin}, #{address}, #{bankAccountId}, #{email})
    </insert>

    <select id="read" parameterType="long" resultType="org">
        SELECT
            id, name, org_form_id, bin, address, bank_account_id, email
        FROM
            ${db.schema}.organizations
        WHERE
            id = #{id}
    </select>

    <update id="update" parameterType="org">
        UPDATE
            ${db.schema}.organizations
        SET
            name = #{name},
            org_form_id = #{orgFormId},
            bin = #{bin},
            address = #{address},
            bank_account_id = #{bankAccountId},
            email = #{email}
        WHERE
            id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM
            ${db.schema}.organizations
        WHERE
            id = #{id}
    </delete>

    <select id="findAll" resultType="org">
        SELECT
            id, name, org_form_id, bin, address, bank_account_id, email
        FROM
            ${db.schema}.organizations
    </select>

</mapper>
